local battler = {}

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
-- BATTLER CREATE FUNCTION --
function battler.start(playerPokemon, opposingPokemon)
    -- set the battle.lua file as the activeScene
    -- local battle = require("battle")
    -- scene.changeTo(battle.lua)       -- Implement fade to and from black for scene transition


    -- while (  both pokemon health > 0, both pokemon are present (nobody fled battle)  )
        -- Start a battle timer (45 seconds to choose an action)
        -- Have the battle scene display its 4 buttons (Attack, Bag, Pokemon, Run) (each button will lead to another sub-button, like how the Attack button will lead to 4 sub-buttons for each of the moves) (right-clicking should get info for a move)
        
end


--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

return battler